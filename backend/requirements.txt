fastapi
uvicorn[standard]
faster-whisper
# 依平台分流：
# - Windows 本機：釘 4.4.0（4.5+ 的 Windows wheel 在 Anaconda 環境會 segfault）
# - Linux/HF Spaces：不釘，由 faster-whisper 拉最新版
#   （4.4.0 的 Linux wheel 會觸發 "cannot enable executable stack" 載入失敗）
ctranslate2==4.4.0 ; platform_system == "Windows"
yt-dlp
pydantic-settings
python-multipart
httpx
# 快速模式把音訊片段編碼成 wav 上傳外部 API 時用到（faster-whisper 已相依，明列以策安全）
numpy
# 簡體→繁體轉換（純 Python、跨平台、無原生相依）
opencc-python-reimplemented
